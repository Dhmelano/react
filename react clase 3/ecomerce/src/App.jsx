//podemos dividir este proyecto de react en 3 partes:

// lo que esta antes de la funcion (se escribe codigo js)

function App() {
  // lo que esta antes del return (se escribe en codigo js)
  return (
    // lo que esta dentro del return
    <h1>inicio proyecto</h1>  // este codigo es como el html pero en realidad es js, por q la carpeta es .js
  )
}

export default App
